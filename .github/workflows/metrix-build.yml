name: Build Chromium

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      fork_branch:
        description: 'Branch to build (defaults to main)'
        required: false
        default: 'main'

jobs:
  build:
    name: Build Chromium
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [self-hosted-linux, self-hosted-windows, self-hosted-macos]
      fail-fast: false

    steps:
      # (1) [OPTIONAL] Checkout "bootstrap" repo if desired
      - name: Checkout bootstrap repository
        uses: actions/checkout@v3
        with:
          path: bootstrap
          fetch-depth: 1

      # (2) Force Python 3.10
      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'   # Ensures Python 3.10 is on PATH

      # (3) Verify Python version
      - name: Verify Python (Linux/macOS)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          echo "Checking Python version on Linux/macOS..."
          python --version
          pip --version

      - name: Verify Python (Windows)
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          Write-Host "Checking Python version on Windows..."
          python --version
          pip --version

      # (4) Enable long paths on Windows & configure Git
      - name: Enable Windows long paths (registry) + Git config
        if: contains(matrix.runner, 'windows')
        shell: powershell
        run: |
          Write-Host "Enabling long paths on Windows..."
          reg add "HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\FileSystem" /v LongPathsEnabled /t REG_DWORD /d 1 /f
          git config --system core.longpaths true

      # (5) Install OS-specific dependencies
      - name: Install Linux dependencies
        if: contains(matrix.runner, 'linux')
        run: |
          sudo apt-get update
          sudo apt-get install -y git python3 python3-pip pkg-config libssl-dev ninja-build \
            ccache libglib2.0-dev libpango1.0-dev libdbus-1-dev libatk1.0-dev libatk-bridge2.0-dev \
            libcups2-dev libxkbcommon-dev libxcomposite-dev libxdamage-dev libxrandr-dev \
            libgbm-dev libasound2-dev libpulse-dev libnss3-dev libxss-dev libgtk-3-dev \
            libx11-xcb-dev  # needed for X11/Xlib-xcb.h

      - name: Install macOS dependencies
        if: contains(matrix.runner, 'macos')
        run: |
          brew install ninja ccache

      - name: Install Windows dependencies
        if: contains(matrix.runner, 'windows')
        shell: powershell
        run: |
          if (!(Get-Command ninja -ErrorAction SilentlyContinue)) {
            choco install ninja -y
          }
          if (!(Get-Command ccache -ErrorAction SilentlyContinue)) {
            choco install ccache -y
          }

      # (6) Clean up old 'chromium' folder
      - name: Clean workspace - Linux/macOS
        if: runner.os != 'Windows'
        run: |
          rm -rf chromium

      - name: Clean workspace - Windows
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          if (Test-Path "chromium") {
            Remove-Item "chromium" -Recurse -Force
          }

      # (7) Install depot_tools
      - name: Install depot_tools (Linux/macOS)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          if [ ! -d "depot_tools" ]; then
            git clone https://chromium.googlesource.com/chromium/tools/depot_tools.git
          else
            cd depot_tools && git pull && cd ..
          fi
          echo "PATH=$PATH:$(pwd)/depot_tools" >> $GITHUB_ENV

      - name: Install depot_tools (Windows)
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          if (!(Test-Path "depot_tools")) {
            git clone https://chromium.googlesource.com/chromium/tools/depot_tools.git
          } else {
            Push-Location .\depot_tools
            git pull
            Pop-Location
          }
          # Add to PATH via GITHUB_ENV
          $newPath = "PATH=$($Env:PATH);$(Resolve-Path .\depot_tools)"
          $newPath | Out-File -FilePath $Env:GITHUB_ENV -Append

      # (8) Setup ccache
      - name: Setup ccache (Linux/macOS)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          mkdir -p ~/.ccache
          echo "max_size = 50G" > ~/.ccache/ccache.conf
          echo "CCACHE_DIR=$(pwd)/.ccache" >> $GITHUB_ENV
          echo "CC=ccache cc" >> $GITHUB_ENV
          echo "CXX=ccache c++" >> $GITHUB_ENV

      # (9) Set fork details
      - name: Set fork details
        shell: bash
        run: |
          FORK_URL="https://github.com/TheTechNetwork/priv-browser.git"
          if [ -n "${{ github.event.inputs.fork_branch }}" ]; then
            FORK_BRANCH="${{ github.event.inputs.fork_branch }}"
          else
            FORK_BRANCH="main"
          fi
          echo "FORK_URL=$FORK_URL" >> $GITHUB_ENV
          echo "FORK_BRANCH=$FORK_BRANCH" >> $GITHUB_ENV
          echo "Using Chromium fork: $FORK_URL ($FORK_BRANCH)"

      # (10) Fetch Chromium source + run hooks
      - name: Fetch Chromium source
        shell: bash
        run: |
          mkdir -p chromium
          cd chromium
          cat > .gclient << EOF
          solutions = [
            {
              "name": "src",
              "url": "$FORK_URL",
              "deps_file": "DEPS",
              "managed": False,
              "custom_deps": {},
              "custom_vars": {},
            },
          ]
          EOF
          
          git clone "$FORK_URL" src
          cd src
          git checkout "$FORK_BRANCH"
          
          # Sync dependencies
          gclient sync --with_branch_heads --with_tags -D
          
          # Run hooks
          gclient runhooks

      # (11) Configure Chromium build
      - name: Configure Chromium build
        shell: bash
        run: |
          cd chromium/src
          mkdir -p out/Release
          gn gen out/Release --args="is_debug=false use_sysroot=false is_component_build=false symbol_level=0"

      # (12) Build Chromium
      - name: Build Chromium
        shell: bash
        run: |
          cd chromium/src
          ninja -C out/Release chrome

      # (13) Package Linux build
      - name: Package Linux build
        if: contains(matrix.runner, 'linux')
        shell: bash
        run: |
          mkdir -p artifacts
          cd chromium/src/out/Release
          mkdir -p chromium-linux
          cp chrome chromium-linux/
          cp -r *.pak chromium-linux/ || true
          cp -r *.bin chromium-linux/ || true
          cp -r locales chromium-linux/ || true
          cp -r resources chromium-linux/ || true
          cp -r MEIPreload chromium-linux/ || true
          cp -r swiftshader chromium-linux/ || true
          cp -r icudtl.dat chromium-linux/ || true
          cp -r *.so chromium-linux/ || true
          
          tar -czf ../../../../artifacts/chromium-linux.tar.gz chromium-linux

      # (14) Package Windows build
      - name: Package Windows build
        if: contains(matrix.runner, 'windows')
        shell: powershell
        run: |
          mkdir -p artifacts
          cd chromium/src/out/Release
          
          mkdir -p chromium-windows
          Copy-Item "chrome.exe" "chromium-windows/" -ErrorAction SilentlyContinue
          Copy-Item "*.pak" "chromium-windows/" -Recurse -ErrorAction SilentlyContinue
          Copy-Item "*.bin" "chromium-windows/" -Recurse -ErrorAction SilentlyContinue
          Copy-Item "locales" "chromium-windows/" -Recurse -ErrorAction SilentlyContinue
          Copy-Item "resources" "chromium-windows/" -Recurse -ErrorAction SilentlyContinue
          Copy-Item "MEIPreload" "chromium-windows/" -Recurse -ErrorAction SilentlyContinue
          Copy-Item "swiftshader" "chromium-windows/" -Recurse -ErrorAction SilentlyContinue
          Copy-Item "icudtl.dat" "chromium-windows/" -ErrorAction SilentlyContinue
          Copy-Item "*.dll" "chromium-windows/" -Recurse -ErrorAction SilentlyContinue
          
          cd ..
          7z a -tzip ../../../artifacts/chromium-windows.zip Release/chromium-windows

      # (15) Package macOS build
      - name: Package macOS build
        if: contains(matrix.runner, 'macos')
        shell: bash
        run: |
          mkdir -p artifacts
          cd chromium/src/out/Release
          ditto -c -k --keepParent Chromium.app ../../../../artifacts/chromium-macos.zip

      # (16) Store artifacts
      - name: Store artifacts
        shell: bash
        run: |
          mkdir -p $GITHUB_WORKSPACE/build-artifacts/${{ matrix.runner }}
          cp -r artifacts/* $GITHUB_WORKSPACE/build-artifacts/${{ matrix.runner }}/ || true
          
          echo "Build date: $(date)" > $GITHUB_WORKSPACE/build-artifacts/${{ matrix.runner }}/build-info.txt
          echo "Branch: $FORK_BRANCH" >> $GITHUB_WORKSPACE/build-artifacts/${{ matrix.runner }}/build-info.txt
          echo "Commit: $(cd chromium/src && git rev-parse HEAD)" >> $GITHUB_WORKSPACE/build-artifacts/${{ matrix.runner }}/build-info.txt
          echo "Runner: ${{ matrix.runner }}" >> $GITHUB_WORKSPACE/build-artifacts/${{ matrix.runner }}/build-info.txt

          echo "::group::Artifacts"
          echo "Artifacts available at: $GITHUB_WORKSPACE/build-artifacts/${{ matrix.runner }}"
          ls -la $GITHUB_WORKSPACE/build-artifacts/${{ matrix.runner }}
          echo "::endgroup::"

          echo "artifacts_path=$GITHUB_WORKSPACE/build-artifacts/${{ matrix.runner }}" >> $GITHUB_OUTPUT
        id: store-artifacts

      # (OPTIONAL) Upload artifacts to GitHub UI
      # - name: Upload build artifacts
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: chromium-${{ matrix.runner }}
      #     path: $GITHUB_WORKSPACE/build-artifacts/${{ matrix.runner }}
